1) docker-compose.yml -> PostgreSQL
2)https://github.com/obra/superpowers
3)Daha iyi bir package nasıl yaparım?
4)LLM'i kullanırken aynı zamanda sorular da sor.
5)Controller.
6)Dbeaver -> docker container'ın içine gir postgresql container'ın
7)docker-hub -> docker-compose
8) pom.xml -> maven -> maven repository
9)Cascade, Fetch Type
10) Test türleri. Unit, integration, endtoend.


docker compose up -d db backend
docker compose up -d --build backend
docker compose up -d db backend prometheus grafana
docker compose logs -f backend
ss -ltnp | grep 3000
npm start
https://www.baeldung.com/java-equals-hashcode-contracts hashcode equals siteye bak.

a)Entity,DTO, Controller, Servis ucu,Repository
b)docker ps, docker logs -f => docker containerlarının durumunu, loglarını vs nasıl anlarım
c)i18n -> internationalization chromedan arasatır.
d)java doc yazdır => comment düş, burada bu yapılıyor amacı bu vs diye.


27 Temmuz:
-id int8 GENERATED BY DEFAULT AS IDENTITY( INCREMENT BY 1 MINVALUE 1 MAXVALUE 9223372036854775807 START 1 CACHE 1 NO CYCLE) -> buradaki keyler niye eklenmiş.
Bigserial hepsini sağlar. Niye int8 yapıyoruz?
-Kolonlar için bu bir primary keydir özelliği koyalım. -> PRIMARY KEY bizim kendi oluşturduğumuz tablolara da primary key gelsin
-CONSTRAINT ukdrgq36x16u3g4q767dwrw6v84 UNIQUE (name) -> Bu kalkacak. Kontrolü arka tarafta sağlayalım.
-Schema creation -> public diye bir schema create edilemeyecek bu bir validation. Ayrıca postgresql'de schema ne demek bak.
-Multiple primary key -> Ya tek bir kolona verirsin ya da composit olur -> hem a hem b kolonunu kullan
-Grafana -> en güncel springboot ve postgresql dashboardlarını koyalım.
-Swagger -> Ayrıntılar koy. Bu uç ne iş yapar, sample, required parametreler vs.








    -id int8 GENERATED BY DEFAULT AS IDENTITY( INCREMENT BY 1 MINVALUE 1 MAXVALUE 9223372036854775807 START 1 CACHE 1 NO CYCLE) -> buradaki keyler niye eklenmiş.
    Bigserial hepsini sağlar. Niye int8 yapıyoruz?
    bigserial: postgreye özgü bir komuttur.
    -kolonu bigint(int8) yapar
    -arkada bir sayaç olusturur.
    -kolonun varsayılan değerini sayactan al olarak ayarlar.
    GENERATED BY DEFAULT AS IDENTITY:
    aynı şeyleri yapar. sql standartlarına daha uygun. sonuclar aynı cıkar.
    https://grafana.com/grafana/dashboards/9845-spring-boot-application/   grafana spring boot dashboard


    Yapılacaklar, temmuz 28:
    -public schema gözükmesin, bu isimle schema da oluşturulmasın
    -composite primary key olacak primary_key(col1,col2)
    -kolon olarak eklemediğin ekstra kolon olmayacak -> id kolonu
    -heap kullanımı, thread kullanımı, aktif veya total connectionlar -> bir springboot servisini grafanada incelemek için ne tür cardlar koyulur
    -Hata kodları düzenlensin. Http codelar ile karışmasın -> 404 not found. Kendi durumlarımız farklı codelar olsun
    -Authentication -> Spring Security ve JWT ile yapalım. Roller ekleyelim farklı kullanıcılara. Kimisi update edebilsin kimisi edemesin.
    -8080'i 8081'e çek. AI'ya yaptırma.
    -i18n backend
    -hibernate tarafında atılan sqlleri console'a yazdırma -> sonra repository katmanına bağlayacağız.
    -Frontend dockerlaştırılacak.








1)Springboot kullanımı + ORM'lerden hibernate kullanımı öğrenilecek. Bir database tasarımının ORM uygulamalarında nasıl gerçekleştirileceği kazanımı amaçlanıyor
2)Bir web uygulamasının katmanları ve isterleri nelerdir -> Controller, Servis, Repository. Bunun yanında i18n, exception handling.
3)Backend Servislerinin Metrik izlenimi -> Prometheus, Grafana, SpringBoot actuator
4)Test Driven Development -> Unit, Integration, Swagger gibi dokümanlarla E2E testleri.
5)Sanallaştırma/Containerization -> Docker kullanılarak hazırlanan imageler ile ortamdan bağımsız deployment süreci
6)Herhangi bir proje end user'a sunulmalı. Web uygulaması, mobil uygulama veya desktop. Bizim amacımız Frontend+Backend ile uçtan uca bir web uygulaması geliştirme
7)Java kritik temelleri ve OOP'yi öğrenme -> getter/setter, class, constructor. java equals/hashcode
8)Agentic Development -> Claude, gemini, ChatGPT gibi llm modelleri günümüzde kodlama için de kullanılıyor. Agentic development ile temel düzeyde requirementları belli olan bir projenin implementationı nasıl yapılır.
9)Maven repo ile library, dependency yönetimi ve docker hub ile docker image yönetimi
10)Authentication kısmı.
